#include "Biblioteca-Lab05.h"

int main() {
    char ***cursos, ***matricula;
    char ***listasDeClases;
    int *codigosAl;

    leerCursos(cursos);
    imprimirCursos(cursos);

    // leerMatricula(codigosAl, matricula);
    // imprimirMatricula(codigosAl, matricula);

    // armarListasDeClases(cursos, matricula, listasDeClases);
    // imprimirListasDeClases(cursos, listasDeClases);

    return 0;
}